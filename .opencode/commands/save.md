---
description: Save the current conversation or a specific insight into the wiki vault as a structured note.
---

This is an OpenCode wrapper around the canonical command prompt in `commands/save.md`.

Read `commands/save.md` and follow it for this request. Keep `commands/save.md` as the source of truth.
