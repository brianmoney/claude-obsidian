---
description: Bootstrap or check the claude-obsidian wiki vault. Reads the wiki skill and runs setup workflow.
---

This is an OpenCode wrapper around the canonical command prompt in `commands/wiki.md`.

Read `commands/wiki.md` and follow it for this request. Keep `commands/wiki.md` as the source of truth.
