---
description: Open, create, or update a visual canvas and add images, text, PDFs, and wiki pages.
---

This is an OpenCode wrapper around the canonical command prompt in `commands/canvas.md`.

Read `commands/canvas.md` and follow it for this request. Keep `commands/canvas.md` as the source of truth.
